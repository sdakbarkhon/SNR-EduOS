-- =====================================================================
-- Migration 155 — комментарий учителя ПО ЭТАПУ проекта.
--
-- КОНТЕКСТ (Большой фикс UI, Фикс 2): у project_submissions уже есть
-- ОДИН teacher_comment на сдачу целиком (миграция 20260619000033_projects.sql,
-- 90/90 заполнено). Промт просит короткий комментарий учителя НА КАЖДЫЙ
-- из 5 этапов (project_stage_progress) — такой колонки не было нигде
-- (разведка: grep всех миграций, трогающих project_stage_progress —
-- 20260619000033/71/72/73/97/20260630000057 — ни одна её не добавляла).
--
-- !!! ЭТА МИГРАЦИЯ НЕ ПРИМЕНЕНА К ПРОД-БАЗЕ ЭТИМ ЗАХОДОМ — та же причина,
-- что и у 153/154 (Этап 12 финал): нет прямого Postgres-подключения/
-- привязанного Supabase CLI в этой среде, только PostgREST/Storage через
-- SUPABASE_SERVICE_ROLE_KEY (не DDL). Ручной шаг заказчика: применить
-- через Supabase Dashboard → SQL Editor.
--
-- RLS НЕ ТРОГАЮ: "teacher reads stage progress" (FOR SELECT) уже покрывает
-- ВСЮ строку, включая новую колонку — учитель сможет читать её сразу
-- после применения миграции. Отдельную UPDATE-политику для учителя (чтобы
-- редактировать комментарий вручную из UI) не добавляю — промт просил
-- только AI-бэкфилл существующих данных, отдельный экран/форма для
-- ручного редактирования комментария учителем по этапу не запрашивались
-- (не выдумываю функционал сверх ТЗ). Если такой экран понадобится
-- позже — тогда и добавить соответствующую UPDATE-политику.
-- =====================================================================

BEGIN;

ALTER TABLE public.project_stage_progress ADD COLUMN teacher_comment text;

COMMIT;
