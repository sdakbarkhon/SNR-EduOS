-- Migration 169: дроп reset_demo_data() (06.08.2026, подготовка к пачке Z)
--
-- reset_demo_data() — последний живой артефакт ПЕРВОГО поколения демо-режима
-- (миграция 20260702000065_demo_reset.sql, 02.07.2026). Она выбирала демо-
-- пользователей по auth.users.raw_user_meta_data->>'is_demo' и массово
-- удаляла их уроки/ДЗ/квизы/уведомления/AI-чаты; вызывалась ночным pg_cron
-- job'ом 'reset-demo-data'.
--
-- Почему её надо убрать, а не оставить «на всякий случай»:
--   * SECURITY DEFINER + ACL '=X/postgres' (EXECUTE для PUBLIC) плюс явные
--     гранты anon и authenticated — то есть её может дёрнуть ЛЮБОЙ
--     анонимный PostgREST-запрос;
--   * сейчас это no-op только по счастливой случайности: миграция 132
--     сняла ключ is_demo из метаданных всех аккаунтов, и функция уходит в
--     ранний return «no demo users found». Ноль пользователей с флагом —
--     единственное, что стоит между чужим анонимным вызовом и массовым
--     DELETE;
--   * пачка Z (разделение демо/настоящий через school_id) будет трогать
--     ровно эту область. Если что-нибудь когда-нибудь снова проставит
--     is_demo в метаданных — спящая дыра станет боевой, без единого
--     предупреждения.
--
-- Проверено live ПЕРЕД дропом (все — пусто):
--   * cron.job — 3 job'а (cleanup-expired-announcements, auto-start-lessons,
--     auto-end-lessons), ни один не ссылается на reset_demo_data;
--     собственный job 'reset-demo-data' был снят ещё миграцией 67;
--   * ни одна другая функция БД не содержит reset_demo_data в теле;
--   * ни один триггер её не использует;
--   * в apps/web и packages нет ни одного вызова (единственное упоминание —
--     строка в сгенерированном packages/core/src/database.types.ts, мёртвая
--     декларация без вызывающих).
--
-- Обратной миграции намеренно нет: восстанавливать функцию первого
-- поколения незачем — механизм is_demo снесён миграцией 132 и НЕ
-- восстанавливается (см. археологию 06.08 в resheniya_2.md).

BEGIN;

DROP FUNCTION IF EXISTS public.reset_demo_data();

COMMIT;
