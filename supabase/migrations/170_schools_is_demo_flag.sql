-- Migration 170: schools.is_demo — флаг-предикат для видимости в /superadmin
-- (Z.1, 06.08.2026)
--
-- ЧТО ЭТО НЕ ТАКОЕ. Это НЕ реставрация механизма is_demo из миграции 110
-- (колонки is_demo на 10 таблицах ДАННЫХ + триггер fn_stamp_is_demo,
-- блокировавший UPDATE/DELETE из демо-сессии). Тот механизм снесён миграцией
-- 132 и восстановлению не подлежит: он был session-scoped против общего пула
-- реальных аккаунтов и структурно не мог работать (см. археологию 06.08 в
-- resheniya_2.md). Здесь — ОДИН булев флаг на ОДНОЙ строке справочника школ,
-- ноль изменений в таблицах данных, ноль триггеров.
--
-- ПОЧЕМУ ЭТО НЕ ПРОТИВОРЕЧИТ МИГРАЦИИ 151. В шапке 151_p2_real_school_gating
-- записано: «RLS ... сама разделяет данные по school_id — никаких is_demo-
-- флагов ... не заводим». Это остаётся верным для той задачи, которую 151
-- решала — ИЗОЛЯЦИЯ ДАННЫХ между арендаторами. Её school_id + RLS
-- действительно решают, и мы это не трогаем.
--
-- Здесь задача другая — ВИДИМОСТЬ ДЕМО-ШКОЛЫ В ИНТЕРФЕЙСЕ СУПЕРАДМИНА, и её
-- RLS решить не может, что подтверждено живыми запросами:
--   * 234 из 254 политик в public несут `OR is_super_admin()` верхним
--     уровнем — суперадмин по построению обходит изоляцию;
--   * у public.schools уже есть PERMISSIVE-политика
--     «authenticated reads schools» USING (true); permissive-политики
--     объединяются через OR, поэтому добавить вторую permissive и «вычесть»
--     ею строки НЕВОЗМОЖНО;
--   * единственный путь на уровне БД — RESTRICTIVE-политика, а их в схеме
--     сейчас ровно 0 (проверено: pg_policies where permissive='RESTRICTIVE').
--     Заводить первый в проекте restrictive-прецедент ради presentation-
--     задачи — несоразмерный риск: он вычислялся бы и для учеников, и для
--     учителей, и для родителей.
-- Поэтому флаг служит ПРЕДИКАТОМ для фильтров в запросах и для серверных
-- проверок в write-действиях, а не механизмом принуждения. RLS не трогаем.
--
-- Заодно флаг убирает хардкод UUID демо-школы, который уже начал расползаться
-- по коду (packages/core/src/queries/index.ts::DEMO_SCHOOL_ID и литерал в
-- миграции 157) — CLAUDE.md §6 прямо просит держать такие вещи одним
-- конфигом, а не по экранам.
--
-- Безопасность изменения (проверено перед написанием):
--   * ни одна RLS-политика не читает колонки schools;
--   * только 2 функции ссылаются на public.schools — fn_auto_start_lessons и
--     fn_auto_end_lessons — и обе читают лишь autostart_enabled;
--   * вьюх над schools нет;
--   * supabase/tests/rls_isolation_school.test.sql вставляет (id, name, code)
--     явно — NOT NULL DEFAULT false её удовлетворяет;
--   * admin-api.ts::createSchool вставляет {name, code, autostart_enabled} —
--     новые школы получают is_demo=false из дефолта, что и требуется.
-- Прецедент формы — миграции 151 и 156, добавлявшие булевы флаги на schools
-- ровно так же.

BEGIN;

ALTER TABLE public.schools
  ADD COLUMN IF NOT EXISTS is_demo boolean NOT NULL DEFAULT false;

UPDATE public.schools
   SET is_demo = true
 WHERE id = 'a0a0a0a0-0000-0000-0000-000000000001'
   AND is_demo IS DISTINCT FROM true;

COMMENT ON COLUMN public.schools.is_demo IS
  'true только у демо-школы (a0a0a0a0-...-0001). Предикат для фильтрации в UI суперадмина и серверных проверок write-действий; НЕ механизм изоляции данных (её делает school_id + RLS) и НЕ реставрация is_demo из миграции 110.';

COMMIT;
