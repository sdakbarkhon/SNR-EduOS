# Expo HAS CHANGED

This app is pinned to Expo SDK ~57.0.8 (see package.json) — NOT v56 (that's apps/mobile, a
different, frozen app in this monorepo, which is the actual v56 project). Read the exact
versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
