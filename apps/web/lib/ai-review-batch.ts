// 08.08.2026 — размер порции AI-проверки ДЗ, общий для клиента и роута.
//
// Число живёт в одном месте намеренно: его знают двое — /api/teacher/homework/
// ai-review/run (отбивает запросы больше порции) и экран запуска (режет
// выбранное на порции). Разойдись они, учитель получал бы 400 на каждой
// второй порции. Копии в этом проекте расходились уже четырежды.
//
// Откуда 8. Потолок функции — maxDuration=300 с (Fluid Compute на Hobby).
// Одна проверка — это вызов Gemini 2.5 Flash с таймаутом 25 с
// (GEMINI_TIMEOUT_MS в lib/ai/homework-review.ts) плюс до одного ретрая,
// то есть худший случай одной работы ~50 с. 8 × 25 = 200 с укладывается в
// потолок с запасом даже без учёта того, что типовая проверка идёт 5–10 с.
// Порция поменьше вдобавок чаще двигает прогресс у учителя.
export const AI_REVIEW_MAX_PER_CALL = 8;
