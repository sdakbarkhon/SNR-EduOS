import { ProgressView } from "./ProgressView";

/**
 * «Успехи» (П10) — веб-порт экрана родительского приложения v2.
 *
 * Экран целиком перенесён 1:1 с apps/mobile-parent (ветка
 * feat/mobile-parent-redesign, ProgressScreen.tsx) и питается фикстурами
 * data-слоя мобилки — apps/web/app/parent/(app)/v2/data. БД к этому экрану
 * СОЗНАТЕЛЬНО не подключена: прежняя серверная версия страницы (Supabase:
 * getStudentGrades / getChildTeacherReviews) закрывала только вкладку
 * «Оценки» — средний балл, разбивку по предметам и один отзыв, — а вёрстка
 * расходилась с утверждённым макетом (вкладки «Навыки»/«Динамика», карточки
 * «Прогресс за неделю»/«Посещаемость», «Сильные стороны / зоны роста» были
 * моком и в мобилке тоже). Подключение реальных данных к новой вёрстке —
 * отдельный шаг, после того как перенос всех экранов v2 будет принят
 * (так же сделано на «Главной», см. home/page.tsx).
 */
export default function ParentProgressPage() {
  return <ProgressView />;
}
