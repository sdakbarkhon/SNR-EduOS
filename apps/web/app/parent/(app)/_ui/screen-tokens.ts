/**
 * Чистые значения общих примитивов родителя: производные цвета и набор SVG-путей.
 *
 * ПОЧЕМУ ОТДЕЛЬНЫЙ ФАЙЛ, А НЕ `screen-kit.tsx`.
 * `screen-kit.tsx` помечен "use client". В React Server Components это значит,
 * что КАЖДЫЙ его экспорт, попадая в серверный компонент, подменяется клиентской
 * ссылкой — функцией-заглушкой из `registerClientReference`. Для компонентов это
 * ровно то, что нужно (их отрисует клиент), а для данных — тихая порча:
 *
 *     typeof ICON            → "function"   (а не "object")
 *     ICON.doc               → undefined    → <Glyph paths={undefined}> падал
 *     `1px solid ${DIVIDER}` → "1px solid function(){throw Error(...)}"
 *     color: WHITE           → тоже функция, CSS-значение невалидно
 *
 * Это измерено, а не выведено: временный статический роут печатал значения на
 * пререндере и показал ровно то, что выше. Ошибки при этом нет ни в tsc (типы
 * совпадают), ни в сборке — экраны просто теряют иконки и разделители, а в
 * худшем случае падают в error boundary.
 *
 * Поэтому всё, что НЕ компонент, живёт здесь, в модуле без "use client", и
 * доступно обеим сторонам. `screen-kit.tsx` эти значения импортирует и
 * реэкспортирует — клиентские экраны продолжают брать их оттуда, как раньше.
 * СЕРВЕРНЫЕ компоненты обязаны импортировать из этого файла напрямую.
 *
 * (Функция `grad135` по той же причине уехала ещё дальше — в `../v2/tokens`,
 * к остальным генераторам градиентов.)
 */

/* ── Производные цвета (в мобилке — внутри соответствующих ui-компонентов) ──
 *
 * ТЁМНАЯ ТЕМА. Как и остальные токены родителя, эти значения применяются
 * инлайн-стилями из серверных компонентов, поэтому хранят не цвет, а ссылку
 * на CSS-переменную; обе схемы заданы в apps/web/app/parent/parent-theme.css
 * и переключаются классом `dark` на <html>. Второй аргумент var() — светлое
 * значение, дословно то, что было здесь до темизации.
 *
 * В darkTokens мобильного эталона этих производных нет — они выведены по
 * правилу «база ink меняется на белую, альфа сохраняется» (в светлой они
 * тёмные rgba поверх белого стекла, в тёмной — светлые поверх тёмного).
 * Исключения расписаны в parent-theme.css.
 */

/** Разделитель строк внутри карточки-списка. */
export const DIVIDER = "var(--p-divider, rgba(23,18,67,0.07))";
/** Цвет SectionHeader — rgba(26,19,74,.5). */
export const SECTION_CAP = "var(--p-section-cap, rgba(26,19,74,0.5))";
/** Шеврон строки. */
export const CHEVRON = "var(--p-chevron, rgba(26,19,74,0.4))";
/** Неактивная пилюля SegmentPills: 160° W60→W40, текст .66 (в тёмной — стоп glass-2). */
export const PILL_INACTIVE_BG =
  "var(--p-pill-inactive-bg, linear-gradient(160deg, rgba(255,255,255,0.6), rgba(255,255,255,0.4)))";
export const PILL_INACTIVE_TEXT = "var(--p-pill-inactive-text, rgba(26,19,74,0.66))";
/** Тень активной пилюли: 0 8 18 rgba(124,58,237,.35); в тёмной — глоу на тёмном accent. */
export const PILL_ACTIVE_SHADOW = "var(--p-pill-active-shadow, 0 8px 18px rgba(124,58,237,0.35))";
/** Онлайн-точка макета: #22C55E + белая обводка 2px (сигнальная заливка, одна в обеих темах). */
export const ONLINE_GREEN = "var(--p-online-green, #22C55E)";
/** Белый текст/глиф на цветной плитке — константа обеих тем, не «цвет текста». */
export const WHITE = "var(--p-white, #FFFFFF)";

/** Часто используемые глифы (те же пути, что в RN-экранах мобилки). */
export const ICON = {
  back: ["M19 12H5", "m12 19-7-7 7-7"],
  chevron: ["m9 18 6-6-6-6"],
  kebab: ["M5 12h.01", "M12 12h.01", "M19 12h.01"],
  bell: ["M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9", "M10.3 21a1.94 1.94 0 0 0 3.4 0"],
  mega: ["m3 11 18-7v16L3 13v-2Z", "M11.6 16.8a3 3 0 1 1-5.8-1.6"],
  chat: ["M7.9 20A9 9 0 1 0 4 16.1L2 22Z"],
  doc: [
    "M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8Z",
    "M14 3v5h5",
    "M9 13h6",
    "M9 17h4",
  ],
  shield: [
    "M20 13c0 5-3.5 7.5-7.7 9a.6.6 0 0 1-.6 0C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.2-2.7a1.2 1.2 0 0 1 1.6 0C14.5 3.8 17 5 19 5a1 1 0 0 1 1 1Z",
    "m9 12 2 2 4-4",
  ],
  check: ["M20 6 9 17l-5-5"],
  checkSquare: [
    "M3 8a5 5 0 0 1 5-5h8a5 5 0 0 1 5 5v8a5 5 0 0 1-5 5H8a5 5 0 0 1-5-5Z",
    "m8.5 12 2.5 2.5 5-5",
  ],
  clock: ["M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18Z", "M12 7v5l3 2"],
  wallet: [
    "M20 12V8H6a2 2 0 0 1 0-4h12v4",
    "M4 6v12a2 2 0 0 0 2 2h14v-6",
    "M18 12a2 2 0 0 0 0 4h4v-4Z",
  ],
  card: ["M2 8a3 3 0 0 1 3-3h14a3 3 0 0 1 3 3v8a3 3 0 0 1-3 3H5a3 3 0 0 1-3-3Z", "M2 10h20"],
  lock: ["M4 11h16v10H4Z", "M8 11V7a4 4 0 0 1 8 0v4"],
  globe: ["M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18Z", "M3 12h18", "M12 3a13 13 0 0 1 0 18", "M12 3a13 13 0 0 0 0 18"],
  help: [
    "M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18Z",
    "M9 10a3 3 0 1 1 4 2.8c-.6.3-1 .9-1 1.7",
    "M12 17h.01",
  ],
  info: ["M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18Z", "M12 8h.01", "M11 12h1v4h1"],
  user: ["M20 21a8 8 0 1 0-16 0", "M12 12a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z"],
  logout: ["M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4", "m16 17 5-5-5-5", "M21 12H9"],
  send: ["m22 2-7 20-4-9-9-4Z", "M22 2 11 13"],
  eye: ["M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7Z", "M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z"],
  pin: ["M12 17v5", "M9 3h6l-1 6 3 3v2H7v-2l3-3-1-6Z"],
  // Заход 2 по оплатам: трубка для строки «Позвонить в школу» в шторке «как
  // оплатить». Ключа с телефоном в наборе не было.
  phone: ["M22 16.9v3a2 2 0 0 1-2.2 2 19.8 19.8 0 0 1-8.6-3.1 19.5 19.5 0 0 1-6-6A19.8 19.8 0 0 1 2 4.2 2 2 0 0 1 4 2h3a2 2 0 0 1 2 1.7c.1 1 .4 1.9.7 2.8a2 2 0 0 1-.5 2.1L8.1 9.9a16 16 0 0 0 6 6l1.3-1.1a2 2 0 0 1 2.1-.5c.9.3 1.8.6 2.8.7a2 2 0 0 1 1.7 2Z"],
  inbox: ["M3 12h5l2 3h4l2-3h5", "M5 5h14l2 7v5a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-5Z"],
} as const;
