// Тот же экран, что у суперадмина. НЕ КОПИЯ, А ПЕРЕ-ЭКСПОРТ: запросы и
// разметка живут в одном месте, разница между ролями — в schoolViewContext.
export { default } from "@/app/superadmin/schools/[id]/view/subjects/page";
